@model IEnumerable<ATNB.Models.TblPublisher>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdminPage.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered" id="tblPub">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PublisherName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th></th>
            <th></th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PublisherName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    <div>
                        <a href="" data-toggle="modal" data-target="#@item.PublisherId">Details</a>
                        <div class="modal fade" id="@item.PublisherId" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h3 class="modal-title" id="exampleModalLabel">Details Publisher</h3>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        @{ Html.RenderPartial("~/Areas/Admin/Views/Publishers/Details.cshtml", item);}
                                        <p type="button" class="glyphicon glyphicon-backward" data-dismiss="modal"></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                <td>
                    <div>
                        <a href="" data-toggle="modal" data-target="#@item.PublisherId-a">Edit</a>
                        <div class="modal fade" id="@item.PublisherId-a" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h3 class="modal-title" id="exampleModalLabel">Editing Publisher</h3>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        @{ Html.RenderPartial("~/Areas/Admin/Views/Publishers/Edit.cshtml", item);}
                                        <p type="button" class="glyphicon glyphicon-backward" data-dismiss="modal"></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </td>
                <td>
                    <div>
                        <a href="" data-toggle="modal" data-target="#@item.PublisherId-b">Delete</a>
                        <div class="modal fade" id="@item.PublisherId-b" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog" role="document">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h3 class="modal-title" id="exampleModalLabel">Confirm Deleting Publisher</h3>
                                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                            <span aria-hidden="true">&times;</span>
                                        </button>
                                    </div>
                                    <div class="modal-body">
                                        @{ Html.RenderPartial("~/Areas/Admin/Views/Publishers/Delete.cshtml", item);}
                                        <p type="button" class="glyphicon glyphicon-backward" data-dismiss="modal"></p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </td>
            </tr>
                                            }
    </tbody>
</table>
